# Image Example

## Setup

#### Start User Interface
```
cd example/image/ui
npm install
npm start
```

#### Start the server
```
cd example/image
go run main.go
```

## Run

Just click on `Start` button and after a while click on `Update` or just set the auto refresh.

## Quit

Just kill the server.
